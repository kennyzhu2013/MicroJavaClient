syntax = "proto3";

package com.kennyzhu.micro.greeter;

service HelloWorld {
    rpc Hello(Request) returns (Response) {}
}

message Request {
    string name = 1;
}

message Response {
    string msg = 1;
}